# 改行はリポジトリ内 LF に統一する（Windows / macOS / CI で diff を出さないため）
* text=auto eol=lf

*.png binary
*.ico binary
*.icns binary
*.woff binary
*.woff2 binary

# 生成物は diff / 言語統計から外す
Cargo.lock       linguist-generated=true
pnpm-lock.yaml   linguist-generated=true -diff
