# PR タイトル（例: P1: MCP サーバ）

<!--
このファイルは PR 説明の雛形。`docs/pr/0004-mcp-server.md` の構成に合わせてある。
コピーして `docs/pr/00XX-<slug>.md` として作り、プレースホルダを埋めること。
1 PR = 1 フェーズの 1 項目（CLAUDE.md）。
-->

## What

<!--
何を実装・変更したかを箇条書きで。「なぜ」は次の節に回し、ここは事実だけ。
対象ファイル・クレート・追加/変更したツールやコマンド名を具体的に書く。
-->

- TODO

## Why

<!--
なぜこの設計にしたか。既存 ADR がある場合はリンクして要点だけ再掲する。
このPRで新しく ADR を書いた/更新した場合はここでも触れる。
-->

TODO

## ツール一覧 / 変更点

<!--
MCP のツールを足す・引数や返り値を変えた PR ではここに表を置く（docs/DESIGN.md §6 と揃える）。
UI や CLI の変更が中心の PR ではこの節ごと削ってよい。
-->

| ツール | 役割 |
|---|---|
| TODO | TODO |

## テストの状況

<!--
実行したコマンドと結果件数を書く。「読んだだけ」ではなく実際に実行した記録にする。
例:
cargo test --workspace 2>&1 | Select-String "test result"
cd apps/desktop; pnpm test
-->

TODO

## レビューで直したもの

<!--
レビュー（自分・他者・reviewer サブエージェント問わず）で指摘され、このPR内で直した点。
無ければ「なし」と書く。
-->

TODO

## 積み残し

<!--
このPRでは触らないと決めた既知の課題。表にして送り先（次のPR番号やフェーズ）を書く。
-->

| 項目 | 送り先 |
|---|---|
| TODO | TODO |

## 関連 ADR

<!--
このPRが従っている、または新しく足した ADR へのリンク。
-->

- TODO

## チェックリスト

- [ ] MCP のツールを足す / 返り値を変えたときは、**実クライアント**（Claude Code か Claude Desktop）から
      実際に呼んで schema validation を通した
- [ ] スクリーンショットに実在のメール・アドレス・社名が写っていない（モックデータのみ）
- [ ] 秘密情報が入っていない（`git diff` に対してパスワード・トークン・接続設定の grep をかけた）
